!!!5
%html
  %head
    %title=title
    %meta{:content => "true", :name => "HandheldFriendly"}/
    %meta{ name: 'apple-mobile-web-app-capable', content: 'yes' }
    %meta{ name: 'apple-mobile-web-app-status-bar-style', content: 'black-translucent' }
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1.0' }
    = stylesheet_link_tag :application, :media => 'all'
  %body{ class: body_class }
    .row
      .col-md-12
        = render 'flashes'
    = yield
    %footer.row
      .col-md-8
        %ul.nav.nav-pills{role: :navigation}
          = tab_link root_path do
            =t('.home')
          = tab_link page_path('pricing') do
            =t('.pricing')
          = tab_link page_path('terms') do
            =t('.terms')
          = tab_link page_path('privacy') do
            =t('.privacy')
          = tab_link page_path('legal') do
            =t('.legal')
      .col-md-4
        %ul.nav.nav-pills.pull-right
          - I18n.available_locales.each do |locale|
            %li{ class: ('active' if locale == I18n.locale) }
              = link_to locale, url_for(locale: locale)
      .col-md-12
        %small SoSlide is a platform to build and host Professional Websites. Each website is simple and fast to make sure your content can be seen everywhere and on all devices.
      .col-md-12
        %small © 2014 Soslide, All Rights Reserved
    - if ENV['SEGMENT_IO'].present?
      :javascript
        window.analytics||(window.analytics=[]),window.analytics.methods=["identify","track","trackLink","trackForm","trackClick","trackSubmit","page","pageview","ab","alias","ready","group","on","once","off"],window.analytics.factory=function(t){return function(){var a=Array.prototype.slice.call(arguments);return a.unshift(t),window.analytics.push(a),window.analytics}};for(var i=0;i<window.analytics.methods.length;i++){var method=window.analytics.methods[i];window.analytics[method]=window.analytics.factory(method)}window.analytics.load=function(t){var a=document.createElement("script");a.type="text/javascript",a.async=!0,a.src=("https:"===document.location.protocol?"https://":"http://")+"d2dq2ahtl5zl1z.cloudfront.net/analytics.js/v1/"+t+"/analytics.min.js";var n=document.getElementsByTagName("script")[0];n.parentNode.insertBefore(a,n)},window.analytics.SNIPPET_VERSION="2.0.8",
        window.analytics.load("#{ENV['SEGMENT_IO']}");
        window.analytics.page();
    = render 'javascript'
